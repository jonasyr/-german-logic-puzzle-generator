# Die Sammlung Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Einen endlichen, durchblätterbaren Katalog aus 120 Rätseln bauen, der aus dem unendlichen Generator ein Heft macht, das man zu Ende bringen kann.

**Architecture:** Ein Katalogeintrag ist fünf Zahlen, kein gespeichertes Rätsel — die Determinismus-Garantie des Generators macht die Bibliothek zu einer eingecheckten Liste. Der Fortschritt wird aus vorhandenen Ergebnissen gelesen; die einzige Server-Änderung ist eine lesende Schnittstelle, weil die bestehende Ergebnisliste bei 100 gedeckelt ist und es 120 Einträge gibt.

**Tech Stack:** Vanilla ES-Module, Vitest 4 (**Node, kein DOM**), Playwright 1.55, Cloudflare Worker + D1/Drizzle, `vendor/logic-puzzle-generator` 1.4.1-logicals.b03a226.

**Spec:** `docs/superpowers/specs/2026-09-19-sammlung-design.md`

## Global Constraints

- **Ein bestehender Katalogeintrag wird nie geändert. Nur angehängt.** Ein geänderter Seed macht den Eintrag für alle, die ihn gelöst haben, still wieder offen.
- **Katalog-Seeds ab 1.000.000.** Das freie Spiel würfelt in 0…99.999.
- **Keine Migration, keine Schemaänderung.** Ergebnisse tragen bereits `seed`, `themeId`, `difficulty` und die Konfiguration.
- **Der Fingerprint hält den Deploy an, nicht den Spieler.** Zur Laufzeit wird nicht geprüft.
- **Spielen braucht kein Netz.** Ohne Verbindung fehlen höchstens die Haken, nie ein Knopf.
- Unit-Tests laufen in **Node ohne DOM**; Oberflächenverhalten wird über Playwright oder Quelltext-Zusicherungen geprüft (Muster: `test/app-shell.test.ts`).
- Oberflächentexte auf Deutsch, Du-Form.
- Commit-Nachrichten ohne `Co-Authored-By:` und ohne jeden KI-Hinweis.
- Alle Shell-Befehle mit `rtk` präfixen, auch in `&&`-Ketten.
- Mindest-Tapfläche 44 × 44 pt.

## Ablauf, Oberfläche, iOS — nicht verhandelbar

Der Auftraggeber nennt den Bedienfluss einen der wichtigsten Vorzüge dieser App.
Jede Aufgabe unten hat diese Anforderungen implizit zu erfüllen; Aufgabe 5
prüft sie ausdrücklich.

**Der Startbildschirm wächst nicht.** Er trug bis heute sieben gleich
aussehende Knöpfe und ist gerade erst auf zwei benannte Gruppen gebracht
worden. Die Sammlung bekommt **genau einen** Eintrag. Wer weiterspielen will,
findet den nächsten offenen Eintrag als *erste* Handlung auf dem
Sammlungsbildschirm — nicht am Ende einer Suche durch zehn Kapitel.

**Kein Tipp ohne Antwort.** Die Erzeugung dauert gemessen bis zu 2,4 Sekunden
(5×5 schwer). Jeder Weg, der erzeugt, zeigt das vorhandene Overlay
(`setBusy` / `clearBusy` in `main.js`) — sonst tippt man und nichts geschieht.

**Keine Zahl, die lügt.** Solange der Stand weder zwischengespeichert noch
geladen ist, steht dort nichts — nicht „0 von 120".

**Zurück bringt einen dahin, wo man war, und zwar aktuell.** Der Router poppt
den Verlauf; der Kapitelbildschirm muss sich beim Betreten neu zeichnen, sonst
behauptet er nach einem gelösten Rätsel, es sei offen.

**iOS Safari:**
- Kopfzeilen folgen dem Muster der bestehenden Bildschirme (`.bar` mit
  `btn--back`), damit die Randwisch-Geste und der Knopf dasselbe tun.
- Lange Listen: `overscroll-behavior: contain`, damit ein Überziehen nicht die
  Seite darunter bewegt.
- Listenzeilen sind echte `<button>`. Damit greifen die Regeln aus `base.css`:
  `touch-action: manipulation` (kein Doppeltipp-Zoom, keine 300-ms-Verzögerung),
  kein grauer Aufblitzer, kein Lange-drücken-Menü.
- Sichtbare Rückmeldung beim Drücken (`:active`), weil ein Tipp, der 2,4 s
  arbeitet, sonst als nicht angekommen gelesen wird.
- Jede Zeile mindestens 44 pt hoch, auch bei 320 px Breite.
- Keine eigenen Scroll-Container ohne Not: jeder zusätzliche `overflow` kappt
  die `touch-action`-Verkettung, wie im Spielbildschirm teuer gelernt.

## Dateistruktur

| Datei | Verantwortung | Aufgabe |
|---|---|---|
| `scripts/build-catalogue.mjs` | **neu** — erzeugt jeden Eintrag einmal, prüft ihn, schreibt die Datei | 1 |
| `client/js/catalogue/catalogue.json` | **neu, erzeugt** — die eingecheckte Liste | 1 |
| `test/catalogue.test.ts` | **neu** — Struktur, Unveränderlichkeit, Generator-Bindung | 1 |
| `scripts/verify-catalogue.mjs` | **neu** — die langsame Echtheitsprüfung (~94 s) | 1 |
| `client/js/catalogue/catalogue.js` | **neu** — reine Lesefunktionen, kein DOM | 2 |
| `test/catalogue-reads.test.ts` | **neu** | 2 |
| `worker/repositories/results.ts` | Repository-Methode | 3 |
| `worker/index.ts` | Route | 3 |
| `client/js/catalogue/solvedSeeds.js` | **neu** — Abruf + lokaler Zwischenspeicher | 4 |
| `client/js/screens/collectionScreen.js` | **neu** — Kapitelübersicht und Kapitelliste | 5 |
| `client/index.html`, `client/js/main.js` | Bildschirme und Verdrahtung | 5, 6 |
| `e2e/collection.spec.ts` | **neu** | 5 |

---

### Task 1: Der Katalog als Datei

**Files:**
- Create: `scripts/build-catalogue.mjs`
- Create: `scripts/verify-catalogue.mjs`
- Create: `client/js/catalogue/catalogue.json` (vom Skript erzeugt)
- Create: `test/catalogue.test.ts`
- Modify: `package.json` (Skripte `catalogue:build`, `catalogue:verify`)

**Interfaces:**
- Produces: `client/js/catalogue/catalogue.json` mit der Gestalt

```json
{
  "generatorVersion": "1.4.1-logicals.b03a226",
  "generatorHash": "<sha256 über vendor/logic-puzzle-generator/dist>",
  "chapters": [
    { "themeId": "streetfood", "title": "Finale beim Street-Food-Festival",
      "entries": [
        { "number": 1, "categoryCount": 3, "valuesPerCategory": 4,
          "difficulty": "leicht", "seed": 1000001,
          "fingerprint": "…", "clueTypes": [0,1,0,2,0,0] }
      ] }
  ]
}
```

- [ ] **Step 1: Schreibe das Bauskript**

`scripts/build-catalogue.mjs`:

```js
/**
 * Baut den Katalog: 10 Kapitel à 12 Einträge.
 *
 * Nicht jeder Seed kommt durch das Qualitätstor des Generators. In einer
 * unendlichen Liste ist das folgenlos - man bekommt das nächste Rätsel. In
 * einem endlichen Kapitel wäre Eintrag 7 für immer tot. Deshalb wird jeder
 * Eintrag hier einmal wirklich erzeugt, und ein Seed, der scheitert, wird
 * übersprungen statt eingetragen.
 */
import { createHash } from 'node:crypto';
import { readdirSync, readFileSync, writeFileSync } from 'node:fs';
import { resolve } from 'node:path';
import {
  generateGermanLogicBooklet, listGermanThemes,
} from '../vendor/logic-puzzle-generator/dist/src/germanBooklet.js';

/** Erst wächst das Gitter, dann die Schwierigkeit. */
const RAMP = [
  [3, 4, 'leicht'], [3, 4, 'leicht'],
  [4, 4, 'leicht'], [4, 4, 'leicht'],
  [4, 4, 'mittel'], [4, 4, 'mittel'],
  [4, 5, 'mittel'], [4, 5, 'mittel'],
  [5, 5, 'mittel'], [5, 5, 'mittel'],
  [5, 5, 'schwer'], [5, 5, 'schwer'],
];

/** Getrennt vom freien Spiel, das in 0…99.999 würfelt. */
const SEED_FLOOR = 1_000_000;

/** Ein Hash über den Generator, damit der Katalog merkt, wenn er sich ändert. */
export function generatorHash(distDir) {
  const hash = createHash('sha256');
  for (const name of readdirSync(distDir, { recursive: true }).sort()) {
    const path = resolve(distDir, String(name));
    try { hash.update(readFileSync(path)); } catch { /* Verzeichnis */ }
  }
  return `sha256:${hash.digest('hex')}`;
}

function buildChapter(theme, chapterIndex) {
  const entries = [];
  let seed = SEED_FLOOR + chapterIndex * 10_000;

  for (const [categoryCount, valuesPerCategory, difficulty] of RAMP) {
    // Solange weitersuchen, bis ein Seed wirklich ein Rätsel ergibt.
    for (;;) {
      seed += 1;
      try {
        const booklet = generateGermanLogicBooklet({
          puzzleCount: 1, categoryCount, valuesPerCategory,
          themeId: theme.id, difficulty, seed, targetCategoryIndex: 1,
        });
        const puzzle = booklet.puzzles[0];
        if (!puzzle) continue;
        entries.push({
          number: entries.length + 1,
          categoryCount, valuesPerCategory, difficulty, seed,
          fingerprint: fingerprintOf(puzzle),
          clueTypes: puzzle.clueTypes,
        });
        break;
      } catch { /* dieser Seed nicht; der nächste */ }
    }
  }
  return { themeId: theme.id, title: theme.title, entries };
}

/**
 * Dieselbe Rechnung wie client/js/generation/canonicalPuzzle.ts.
 *
 * Bewusst hier wiederholt statt importiert: das Skript läuft in Node, die
 * Client-Fassung nutzt die WebCrypto-API des Browsers.
 */
function fingerprintOf(puzzle) {
  const canonical = JSON.stringify({
    categories: puzzle.categories.map(c => ({ label: c.label, values: c.values })),
    clues: puzzle.clues,
    solutionRows: puzzle.solutionRows,
  });
  return createHash('sha256').update(canonical).digest('hex');
}

const root = resolve(import.meta.dirname, '..');
const distDir = resolve(root, 'vendor/logic-puzzle-generator/dist');
const version = JSON.parse(
  readFileSync(resolve(root, 'vendor/logic-puzzle-generator/package.json'), 'utf8')).version;

const catalogue = {
  generatorVersion: version,
  generatorHash: generatorHash(distDir),
  chapters: listGermanThemes().map(buildChapter),
};

writeFileSync(
  resolve(root, 'client/js/catalogue/catalogue.json'),
  `${JSON.stringify(catalogue, null, 2)}\n`,
);
console.log(`${catalogue.chapters.length} Kapitel, `
  + `${catalogue.chapters.reduce((n, c) => n + c.entries.length, 0)} Einträge`);
```

> **Wichtig:** `fingerprintOf` muss dasselbe berechnen wie
> `client/js/generation/canonicalPuzzle.ts`. Lies diese Datei zuerst und
> übernimm ihre Kanonisierung **wörtlich**; weicht sie ab, ist der
> Fingerprint im Katalog wertlos. Stimmt die Form nicht überein, passe das
> Skript an die Client-Fassung an, nicht umgekehrt.

- [ ] **Step 2: Baue den Katalog**

```bash
rtk mkdir -p client/js/catalogue && rtk node scripts/build-catalogue.mjs
```

Erwartet: `10 Kapitel, 120 Einträge`, Laufzeit rund 94 Sekunden.

- [ ] **Step 3: Schreibe die schnellen Tests**

`test/catalogue.test.ts`:

```ts
import { createHash } from 'node:crypto';
import { readdirSync, readFileSync } from 'node:fs';
import { resolve } from 'node:path';
import { describe, expect, it } from 'vitest';

const catalogue = JSON.parse(
  readFileSync(resolve('client/js/catalogue/catalogue.json'), 'utf8'));

const RAMP = [
  [3, 4, 'leicht'], [3, 4, 'leicht'], [4, 4, 'leicht'], [4, 4, 'leicht'],
  [4, 4, 'mittel'], [4, 4, 'mittel'], [4, 5, 'mittel'], [4, 5, 'mittel'],
  [5, 5, 'mittel'], [5, 5, 'mittel'], [5, 5, 'schwer'], [5, 5, 'schwer'],
];

describe('Katalog', () => {
  it('hat zehn Kapitel mit je zwölf Einträgen', () => {
    expect(catalogue.chapters).toHaveLength(10);
    for (const chapter of catalogue.chapters) {
      expect(chapter.entries, chapter.themeId).toHaveLength(12);
    }
  });

  it('folgt in jedem Kapitel derselben Steigerung', () => {
    for (const chapter of catalogue.chapters) {
      const shape = chapter.entries.map((e: any) =>
        [e.categoryCount, e.valuesPerCategory, e.difficulty]);
      expect(shape, chapter.themeId).toEqual(RAMP);
    }
  });

  it('hält die Seeds vom freien Spiel getrennt', () => {
    // Das freie Spiel würfelt in 0…99.999. Ohne getrennte Bereiche bekäme man
    // einen Kapiteleintrag gutgeschrieben, den man zufällig getroffen hat.
    for (const chapter of catalogue.chapters) {
      for (const entry of chapter.entries) {
        expect(entry.seed, `${chapter.themeId} ${entry.number}`)
          .toBeGreaterThanOrEqual(1_000_000);
      }
    }
  });

  it('vergibt jeden Seed nur einmal', () => {
    const seeds = catalogue.chapters.flatMap((c: any) =>
      c.entries.map((e: any) => e.seed));
    expect(new Set(seeds).size).toBe(seeds.length);
  });

  /*
   * Der Katalog kann nur ungültig werden, wenn sich der Generator ändert.
   * Diese Prüfung kostet Millisekunden und ersetzt damit im Alltag die
   * langsame Echtheitsprüfung: Ändert sich vendor/, wird sie rot und verweist
   * auf `npm run catalogue:verify`.
   */
  it('ist an genau diesen Generator gebunden', () => {
    const version = JSON.parse(readFileSync(
      resolve('vendor/logic-puzzle-generator/package.json'), 'utf8')).version;
    expect(catalogue.generatorVersion,
      'Generator-Version geändert - führe `npm run catalogue:verify` aus').toBe(version);

    const distDir = resolve('vendor/logic-puzzle-generator/dist');
    const hash = createHash('sha256');
    for (const name of readdirSync(distDir, { recursive: true }).sort()) {
      try { hash.update(readFileSync(resolve(distDir, String(name)))); } catch { /* Verzeichnis */ }
    }
    expect(catalogue.generatorHash,
      'Generator-Dateien geändert - führe `npm run catalogue:verify` aus')
      .toBe(`sha256:${hash.digest('hex')}`);
  });
});
```

- [ ] **Step 4: Schreibe den Abzug gegen stille Änderungen**

Ergänze in `test/catalogue.test.ts`:

```ts
/*
 * Ein geänderter Seed macht den Eintrag für alle, die ihn gelöst haben, still
 * wieder offen - ohne Fehlermeldung, ohne Spur. Nur Anhängen ist erlaubt.
 */
it('ändert keinen bestehenden Eintrag', () => {
  const tuples = catalogue.chapters.flatMap((c: any) => c.entries.map((e: any) =>
    `${c.themeId}|${e.number}|${e.categoryCount}x${e.valuesPerCategory}|${e.difficulty}|${e.seed}`));
  expect(tuples).toMatchSnapshot();
});
```

- [ ] **Step 5: Schreibe die langsame Echtheitsprüfung**

`scripts/verify-catalogue.mjs`:

```js
/**
 * Erzeugt jeden Katalogeintrag neu und vergleicht den Fingerprint.
 *
 * Gemessen rund 94 Sekunden. Läuft deshalb nicht in `npm test`, sondern auf
 * Zuruf - und `test/catalogue.test.ts` sorgt dafür, dass man den Zuruf nicht
 * vergessen kann: sobald sich der Generator rührt, wird dort rot.
 */
import { readFileSync } from 'node:fs';
import { resolve } from 'node:path';
import { createHash } from 'node:crypto';
import { generateGermanLogicBooklet } from '../vendor/logic-puzzle-generator/dist/src/germanBooklet.js';

const catalogue = JSON.parse(readFileSync(
  resolve(import.meta.dirname, '../client/js/catalogue/catalogue.json'), 'utf8'));

// Dieselbe Kanonisierung wie in scripts/build-catalogue.mjs.
function fingerprintOf(puzzle) {
  return createHash('sha256').update(JSON.stringify({
    categories: puzzle.categories.map(c => ({ label: c.label, values: c.values })),
    clues: puzzle.clues,
    solutionRows: puzzle.solutionRows,
  })).digest('hex');
}

let checked = 0;
const broken = [];
for (const chapter of catalogue.chapters) {
  for (const entry of chapter.entries) {
    try {
      const booklet = generateGermanLogicBooklet({
        puzzleCount: 1, categoryCount: entry.categoryCount,
        valuesPerCategory: entry.valuesPerCategory, themeId: chapter.themeId,
        difficulty: entry.difficulty, seed: entry.seed, targetCategoryIndex: 1,
      });
      const actual = fingerprintOf(booklet.puzzles[0]);
      if (actual !== entry.fingerprint) {
        broken.push(`${chapter.themeId} #${entry.number}: Fingerprint weicht ab`);
      }
    } catch (error) {
      broken.push(`${chapter.themeId} #${entry.number}: ${error.message}`);
    }
    checked += 1;
  }
}

console.log(`${checked} Einträge geprüft, ${broken.length} abweichend`);
for (const line of broken) console.log('  ' + line);
process.exit(broken.length === 0 ? 0 : 1);
```

- [ ] **Step 6: Trage die Skripte ein**

In `package.json`, bei `scripts`:

```json
"catalogue:build": "node scripts/build-catalogue.mjs",
"catalogue:verify": "node scripts/verify-catalogue.mjs"
```

- [ ] **Step 7: Lass beides laufen**

```bash
rtk npm test -- catalogue && rtk npm run catalogue:verify
```

Erwartet: Tests grün; Prüfung meldet `120 Einträge geprüft, 0 abweichend`.

- [ ] **Step 8: Belege, dass die Generator-Bindung greift**

Ändere probeweise `catalogue.generatorHash` in der JSON auf `sha256:0`, lass
`rtk npm test -- catalogue` laufen und überzeuge dich, dass der Test rot wird
und auf `npm run catalogue:verify` verweist. Stelle die Datei danach wieder her
(`rtk git checkout client/js/catalogue/catalogue.json`).

Eine Prüfung, deren Versagen nie beobachtet wurde, ist keine.

- [ ] **Step 9: Commit**

```bash
rtk git add scripts/ client/js/catalogue/ test/catalogue.test.ts package.json && \
rtk git commit -m "feat(catalogue): build the finite booklet from the infinite generator

Ten chapters, twelve entries each, climbing from 3x4 leicht to 5x5 schwer. An
entry is five numbers rather than a stored puzzle: the generator is
deterministic, so the library costs a list.

Every entry is generated once at build time, because not every seed clears the
quality gate. In an infinite list a failure costs nothing; in a finite chapter
entry seven would be dead forever.

The catalogue pins the generator by version and by a hash over its files. That
check costs milliseconds and runs in npm test, so the slow verification
(~94s, on demand) cannot be forgotten: the moment vendor/ moves, the fast test
goes red and names the command."
```

---

### Task 2: Die Lesefunktionen

**Files:**
- Create: `client/js/catalogue/catalogue.js`
- Create: `test/catalogue-reads.test.ts`

**Interfaces:**
- Consumes: `client/js/catalogue/catalogue.json` aus Aufgabe 1
- Produces:
  - `CATALOGUE_SEED_FLOOR: 1_000_000`
  - `chapters(): Chapter[]` mit `Chapter = { themeId, title, entries: Entry[] }`
  - `Entry = { number, categoryCount, valuesPerCategory, difficulty, seed, fingerprint, clueTypes }`
  - `chapterFor(themeId): Chapter | null`
  - `progressOf(chapter, solved: Set<number>): { solved: number, total: number }`
  - `totalProgress(solved: Set<number>): { solved: number, total: number }`
  - `nextOpen(chapter, solved: Set<number>): Entry | null`
  - `optionsFor(chapter, entry): object` — die Optionen für `fetchBooklet`
  - `newClueTypeAt(chapter, index): string | null`

- [ ] **Step 1: Schreibe die fehlschlagenden Tests**

`test/catalogue-reads.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import {
  chapterFor, chapters, newClueTypeAt, nextOpen, optionsFor, progressOf, totalProgress,
} from '../client/js/catalogue/catalogue.js';

const first = () => chapters()[0];

describe('Katalog lesen', () => {
  it('zählt den Fortschritt je Kapitel', () => {
    const chapter = first();
    const solved = new Set([chapter.entries[0].seed, chapter.entries[3].seed]);
    expect(progressOf(chapter, solved)).toEqual({ solved: 2, total: 12 });
  });

  it('zählt den Fortschritt über alles', () => {
    expect(totalProgress(new Set())).toEqual({ solved: 0, total: 120 });
  });

  it('schlägt den ersten offenen Eintrag vor, nicht den nächsten nach dem letzten gelösten', () => {
    // Springen ist erlaubt, also kann eine Lücke hinter einem gelösten Eintrag
    // liegen. Der Vorschlag muss die Lücke finden, nicht das Ende.
    const chapter = first();
    const solved = new Set([chapter.entries[0].seed, chapter.entries[2].seed]);
    expect(nextOpen(chapter, solved)?.number).toBe(2);
  });

  it('gibt nichts zurück, wenn ein Kapitel fertig ist', () => {
    const chapter = first();
    expect(nextOpen(chapter, new Set(chapter.entries.map(e => e.seed)))).toBe(null);
  });

  it('baut die Generator-Optionen aus dem Eintrag', () => {
    const chapter = first();
    const entry = chapter.entries[4];
    expect(optionsFor(chapter, entry)).toEqual({
      puzzleCount: 1,
      categoryCount: entry.categoryCount,
      valuesPerCategory: entry.valuesPerCategory,
      themeId: chapter.themeId,
      difficulty: entry.difficulty,
      targetCategoryIndex: 1,
      seed: entry.seed,
    });
  });

  it('nennt eine Hinweisart nur dort, wo sie zum ersten Mal vorkommt', () => {
    const chapter = first();
    // Eintrag 1 führt die Grundarten ein, aber "neu" zu sagen ist dort sinnlos.
    expect(newClueTypeAt(chapter, 0)).toBe(null);
    // Irgendwo im Kapitel muss BETWEEN zum ersten Mal auftauchen.
    const mentions = chapter.entries
      .map((_, index) => newClueTypeAt(chapter, index))
      .filter(Boolean);
    expect(mentions.length).toBeGreaterThan(0);
    // Und keine Art wird zweimal als neu angekündigt.
    expect(new Set(mentions).size).toBe(mentions.length);
  });

  it('kennt kein Kapitel, das es nicht gibt', () => {
    expect(chapterFor('gibtsnicht')).toBe(null);
  });
});
```

- [ ] **Step 2: Lass sie fehlschlagen**

```bash
rtk npm test -- catalogue-reads
```

Erwartet: FAIL, `Cannot find module '../client/js/catalogue/catalogue.js'`.

- [ ] **Step 3: Schreibe das Modul**

`client/js/catalogue/catalogue.js`:

```js
/**
 * Reine Lesefunktionen über dem Katalog. Kein DOM, kein Netz.
 *
 * Der Katalog selbst ist eine eingecheckte Datei; die Rechnerei darüber ist
 * Logik und gehört deshalb dorthin, wo sie ohne Browser prüfbar ist.
 */

import data from './catalogue.json' with { type: 'json' };

export const CATALOGUE_SEED_FLOOR = 1_000_000;

/** Kurzform der Hinweisarten, in derselben Reihenfolge wie das ClueType-Enum. */
const CLUE_TYPE_NAMES = [
  'A ist B',                       // 0 BINARY
  'A liegt vor B',                 // 1 ORDINAL
  'A ist die äußerste',            // 2 SUPERLATIVE
  'A hat eine Eigenschaft',        // 3 UNARY
  'zwei Skalen im Verhältnis',     // 4 CROSS_ORDINAL
  'A liegt zwischen B und C',      // 5 BETWEEN
  'A und B sind benachbart',       // 6 ADJACENCY
  'mindestens eines von beiden',   // 7 OR
  'gleiche Differenz',             // 8 ARITHMETIC
];

/**
 * Arten, die schon der erste Eintrag mitbringt. Sie als "neu" anzukündigen
 * wäre keine Auskunft, sondern Lärm.
 */
function baseTypes(chapter) {
  return new Set(chapter.entries[0].clueTypes);
}

export function chapters() { return data.chapters; }

export function chapterFor(themeId) {
  return data.chapters.find(chapter => chapter.themeId === themeId) ?? null;
}

export function progressOf(chapter, solved) {
  return {
    solved: chapter.entries.filter(entry => solved.has(entry.seed)).length,
    total: chapter.entries.length,
  };
}

export function totalProgress(solved) {
  return data.chapters.reduce((sum, chapter) => {
    const part = progressOf(chapter, solved);
    return { solved: sum.solved + part.solved, total: sum.total + part.total };
  }, { solved: 0, total: 0 });
}

/**
 * Der erste offene Eintrag - nicht der nach dem letzten gelösten.
 *
 * Springen ist erlaubt, also können Lücken hinter gelösten Einträgen liegen.
 * Der Vorschlag soll die Lücke schließen, nicht ans Ende zeigen.
 */
export function nextOpen(chapter, solved) {
  return chapter.entries.find(entry => !solved.has(entry.seed)) ?? null;
}

export function optionsFor(chapter, entry) {
  return {
    puzzleCount: 1,
    categoryCount: entry.categoryCount,
    valuesPerCategory: entry.valuesPerCategory,
    themeId: chapter.themeId,
    difficulty: entry.difficulty,
    targetCategoryIndex: 1,
    seed: entry.seed,
  };
}

/**
 * Die Hinweisart, die an dieser Stelle zum ersten Mal auftaucht - oder nichts.
 *
 * Das ist der Trainingsanteil der Sammlung, und er kostet nichts: welche Arten
 * vorkommen, entscheidet ohnehin die Schwierigkeitsstufe. Die App sagt es nur
 * endlich laut.
 */
export function newClueTypeAt(chapter, index) {
  const seen = baseTypes(chapter);
  for (let position = 1; position < index; position += 1) {
    for (const type of chapter.entries[position].clueTypes) seen.add(type);
  }
  if (index === 0) return null;
  for (const type of chapter.entries[index].clueTypes) {
    if (!seen.has(type)) return CLUE_TYPE_NAMES[type] ?? null;
  }
  return null;
}
```

- [ ] **Step 4: Lass sie laufen**

```bash
rtk npm test -- catalogue-reads
```

Erwartet: alle grün. Falls `newClueTypeAt` keine Nennungen findet, prüfe die
`clueTypes` im gebauten Katalog — die Steigerung muss neue Arten einführen,
sonst stimmt die Rampe nicht.

- [ ] **Step 5: Commit**

```bash
rtk git add client/js/catalogue/catalogue.js test/catalogue-reads.test.ts && \
rtk git commit -m "feat(catalogue): read progress and the next open entry

No DOM and no network here, so the arithmetic is testable in the same node
environment as everything else.

Two decisions worth naming. The suggested entry is the first open one, not the
one after the last solved: jumping is allowed, so gaps sit behind solved
entries and pointing at the end would skip them. And a clue type is announced
only where it first appears - the difficulty already decides which types occur,
so saying it on every entry would be noise rather than information."
```

---

### Task 3: Die gelösten Seeds vom Server

**Files:**
- Modify: `worker/repositories/results.ts` (Interface + D1-Implementierung)
- Modify: `worker/index.ts` (Route)
- Modify: `test/results.test.ts` (`MemoryResults`)
- Test: `test/sqlite-repositories.test.ts`

**Interfaces:**
- Produces: `listSolvedSeeds(playerId: number, minSeed: number): Promise<number[]>` auf `ResultRepository`; Route `GET /api/players/:id/solved-seeds` → `{ seeds: number[] }`

- [ ] **Step 1: Schreibe den fehlschlagenden Test**

Ergänze in `test/sqlite-repositories.test.ts`:

```ts
it('gibt nur die Seeds aus dem Katalogbereich zurück, jeden einmal', async () => {
  // Der Sammlungs-Bildschirm braucht 120 Zustände, die Ergebnisliste ist bei
  // 100 gedeckelt. Deshalb eine eigene, schmale Abfrage: nur Zahlen.
  await insertResult(repository, { playerId: 1, seed: 1000001, attemptKey: 'a' });
  await insertResult(repository, { playerId: 1, seed: 42, attemptKey: 'b' });
  await insertResult(repository, { playerId: 1, seed: 1000001, attemptKey: 'c' });
  await insertResult(repository, { playerId: 2, seed: 1000009, attemptKey: 'd' });

  expect(await repository.listSolvedSeeds(1, 1_000_000)).toEqual([1000001]);
});
```

> Nutze den in dieser Datei bereits vorhandenen Einfüge-Helfer. Heißt er
> anders als `insertResult`, übernimm den vorhandenen Namen; lege keinen
> zweiten an.

- [ ] **Step 2: Lass ihn fehlschlagen**

```bash
rtk npm test -- sqlite-repositories
```

Erwartet: FAIL, `repository.listSolvedSeeds is not a function`.

- [ ] **Step 3: Erweitere Interface und D1-Implementierung**

In `worker/repositories/results.ts`, im Interface `ResultRepository`:

```ts
  /**
   * Die Seeds, die dieser Spieler aus dem Katalogbereich gelöst hat.
   *
   * Eigene Abfrage statt listByPlayer, weil die Sammlung 120 Zustände braucht
   * und die Ergebnisliste bei 100 gedeckelt ist - und weil hier nur Zahlen
   * gebraucht werden, keine Ergebniszeilen.
   */
  listSolvedSeeds(playerId: number, minSeed: number): Promise<number[]>;
```

Und in `createResultsRepository`:

```ts
    async listSolvedSeeds(playerId, minSeed) {
      const { results } = await db.prepare(`
        SELECT DISTINCT seed FROM results
        WHERE player_id = ? AND seed >= ?
        ORDER BY seed
      `).bind(playerId, minSeed).all<{ seed: number }>();
      return (results ?? []).map(row => row.seed);
    },
```

- [ ] **Step 4: Zieh das Test-Double nach**

In `test/results.test.ts`, in `class MemoryResults`:

```ts
  async listSolvedSeeds(playerId: number, minSeed: number) {
    const seeds = this.rows
      .filter(row => row.playerId === playerId && row.seed >= minSeed)
      .map(row => row.seed);
    return [...new Set(seeds)].sort((a, b) => a - b);
  }
```

> Der Feldname der Sammlung in `MemoryResults` kann von `this.rows` abweichen.
> Lies die Klasse und benutze ihren vorhandenen Namen.

- [ ] **Step 5: Lass den Test laufen**

```bash
rtk npm test -- sqlite-repositories results
```

Erwartet: grün.

- [ ] **Step 6: Schreibe die Route**

In `worker/index.ts`, direkt nach dem Block mit `historyMatch`:

```ts
        const solvedMatch = url.pathname.match(/^\/api\/players\/(\d+)\/solved-seeds$/);
        if (solvedMatch && request.method === 'GET') {
          // Nur der Katalogbereich: das freie Spiel würfelt in 0…99.999, und
          // seine Seeds gehen die Sammlung nichts an.
          const seeds = await resultsFor(env, overrides)
            .listSolvedSeeds(Number(solvedMatch[1]), 1_000_000);
          return json({ seeds });
        }
```

- [ ] **Step 7: Prüfe den ganzen Worker**

```bash
rtk npm test && rtk npx tsc --noEmit
```

- [ ] **Step 8: Commit**

```bash
rtk git add worker/ test/ && rtk git commit -m "feat(api): expose the catalogue seeds a player has solved

The collection needs 120 states and the result list is capped at 100 - the
worker answers 400 above that rather than clamping. Rather than lifting a cap
that exists for a reason, this is a narrower question with a narrower answer:
a sorted list of integers from the catalogue seed range, no result rows.

No migration. Results already carry the seed."
```

---

### Task 4: Die gelösten Seeds im Client

**Files:**
- Create: `client/js/catalogue/solvedSeeds.js`
- Test: `test/solved-seeds.test.ts`

**Interfaces:**
- Consumes: `GET /api/players/:id/solved-seeds` aus Aufgabe 3
- Produces: `loadSolvedSeeds(playerId): Promise<Set<number>>`, `cachedSolvedSeeds(playerId): Set<number>`

- [ ] **Step 1: Schreibe den fehlschlagenden Test**

`test/solved-seeds.test.ts`:

```ts
import { readFileSync } from 'node:fs';
import { resolve } from 'node:path';
import { describe, expect, it } from 'vitest';

/*
 * Node ohne DOM: geprüft wird die Absicht am Quelltext, nach dem Muster von
 * test/app-shell.test.ts. Das Verhalten selbst deckt e2e/collection.spec.ts ab.
 */
const source = readFileSync(resolve('client/js/catalogue/solvedSeeds.js'), 'utf8');

describe('gelöste Seeds', () => {
  it('legt den Zwischenspeicher je Spieler an', () => {
    // Ein gemeinsamer Schlüssel würde einem Spieler die Haken eines anderen
    // zeigen - auf einem Gerät, das sich zwei Leute teilen, sofort sichtbar.
    expect(source).toMatch(/logicals\.solvedSeeds\.v1\.\$\{playerId\}|`logicals\.solvedSeeds\.v1\.\$\{/);
  });

  it('gibt bei totem Netz den Zwischenspeicher zurück statt zu werfen', () => {
    // Ohne Netz darf die Sammlung Haken verlieren, aber nicht unbenutzbar sein.
    expect(source).toContain('catch');
    expect(source).toContain('cachedSolvedSeeds');
  });
});
```

- [ ] **Step 2: Lass ihn fehlschlagen**

```bash
rtk npm test -- solved-seeds
```

Erwartet: FAIL, `ENOENT … solvedSeeds.js`.

- [ ] **Step 3: Schreibe das Modul**

`client/js/catalogue/solvedSeeds.js`:

```js
/**
 * Welche Katalogeinträge dieser Spieler gelöst hat.
 *
 * Zusätzlich lokal abgelegt, weil die Sammlung sonst ohne Netz leer aussähe -
 * und eine leere Sammlung ist eine schlechtere Auskunft als eine veraltete.
 * Spielen selbst braucht ohnehin kein Netz.
 */

const KEY = playerId => `logicals.solvedSeeds.v1.${playerId}`;

export function cachedSolvedSeeds(playerId) {
    try {
        const raw = localStorage.getItem(KEY(playerId));
        return new Set(raw ? JSON.parse(raw) : []);
    } catch {
        // Privater Modus oder beschädigter Speicher: dann eben ohne Haken.
        return new Set();
    }
}

export async function loadSolvedSeeds(playerId) {
    let response;
    try {
        response = await fetch(`/api/players/${playerId}/solved-seeds`);
        if (!response.ok) throw new Error('nicht erreichbar');
    } catch {
        return cachedSolvedSeeds(playerId);
    }

    let seeds;
    try {
        seeds = (await response.json()).seeds ?? [];
    } catch {
        return cachedSolvedSeeds(playerId);
    }

    try { localStorage.setItem(KEY(playerId), JSON.stringify(seeds)); }
    catch { /* nicht merken können ist kein Grund, nichts anzuzeigen */ }
    return new Set(seeds);
}
```

- [ ] **Step 4: Lass ihn laufen**

```bash
rtk npm test -- solved-seeds
```

- [ ] **Step 5: Commit**

```bash
rtk git add client/js/catalogue/solvedSeeds.js test/solved-seeds.test.ts && \
rtk git commit -m "feat(catalogue): remember the solved seeds for offline

The collection would look empty on a train otherwise, and an empty collection
is a worse answer than a stale one - playing needs no network at all. Cached
per player, because a shared key would show one player the other's ticks on a
device they share."
```

---

### Task 5: Die beiden Bildschirme

**Files:**
- Create: `client/js/screens/collectionScreen.js`
- Modify: `client/index.html` (zwei Abschnitte)
- Modify: `client/js/main.js` (Verdrahtung)
- Modify: `e2e/support/layoutGuard.ts` (die neuen Bildschirme)
- Create: `e2e/collection.spec.ts`

**Interfaces:**
- Consumes: alles aus den Aufgaben 2 und 4; `openPlay(puzzle, context)` aus `client/js/play/playController.js`; `fetchBooklet(options)` aus `client/js/api.js`
- Produces: `initCollection({ onOpenPlay })`, `openCollection(player)`

- [ ] **Step 1: Schreibe den fehlschlagenden Ablauf-Test**

`e2e/collection.spec.ts`:

```ts
import { expect, test, type Page } from '@playwright/test';

/*
 * Die Sammlung macht aus dem Generator ein Heft: endlich, durchblätterbar,
 * abhakbar. Geprüft wird der Weg, auf den es ankommt - ein Eintrag gelöst, und
 * der Haken steht überall, bis hinauf zum Startbildschirm.
 */

async function ada(page: Page, solvedSeeds: number[] = []) {
  await page.addInitScript(() => {
    localStorage.setItem('logicals.players.v1', JSON.stringify({
      players: [{ id: 1, displayName: 'Ada' }], selectedPlayerId: 1 }));
    localStorage.setItem('logicals.seenIntro.v1', '1');
  });
  await page.route('**/api/players', route => route.fulfill({
    status: 200, contentType: 'application/json',
    body: JSON.stringify({ players: [{ id: 1, displayName: 'Ada', createdAt: '2026-09-17T00:00:00Z' }] }),
  }));
  await page.route('**/api/players/*/results**', route => route.fulfill({
    status: 200, contentType: 'application/json', body: JSON.stringify({ results: [] }),
  }));
  await page.route('**/api/players/*/solved-seeds', route => route.fulfill({
    status: 200, contentType: 'application/json', body: JSON.stringify({ seeds: solvedSeeds }),
  }));
}

test('der Startbildschirm zeigt den Stand der Sammlung', async ({ page }) => {
  await ada(page, [1000001, 1000002]);
  await page.goto('/');
  await expect(page.locator('#collection-detail')).toContainText('2 von 120');
});

test('ein Kapitel führt in ein Rätsel und der Haken bleibt', async ({ page }) => {
  test.setTimeout(240_000);
  await ada(page);
  await page.goto('/');
  await page.locator('#collection-button').click();
  await expect(page.locator('#screen-collection')).toHaveClass(/is-active/);

  // Erstes Kapitel, erster Eintrag: 3x4 leicht, in Sekunden erzeugt.
  await page.locator('.chapter-row').first().click();
  await expect(page.locator('#screen-chapter')).toHaveClass(/is-active/);
  await expect(page.locator('.entry-row')).toHaveCount(12);

  await page.locator('.entry-row').first().click();
  await expect(page.locator('#overview-canvas')).toBeVisible({ timeout: 120_000 });
});

test('ohne Netz bleibt die Sammlung benutzbar', async ({ page, context }) => {
  await ada(page, [1000001]);
  await page.goto('/');
  await expect(page.locator('#collection-detail')).toContainText('1 von 120');

  await context.setOffline(true);
  try {
    await page.reload();
    await page.locator('#collection-button').click();
    // Der Haken kommt aus dem Zwischenspeicher, nicht vom Server.
    await expect(page.locator('#screen-collection')).toHaveClass(/is-active/);
    await expect(page.locator('.chapter-row').first()).toContainText('1/12');
  } finally {
    await context.setOffline(false);
  }
});
```

- [ ] **Step 2: Lass ihn fehlschlagen**

```bash
rtk npx playwright test e2e/collection.spec.ts --reporter=line
```

Erwartet: FAIL, `#collection-detail` existiert nicht.

- [ ] **Step 3: Baue die zwei Abschnitte im HTML**

In `client/index.html`, vor `<!-- 8. Spielen -->`:

```html
    <!-- Die Sammlung: Kapitelübersicht und ein Kapitel. -->
    <section class="screen screen--history" id="screen-collection">
        <header class="bar">
            <button class="btn btn--ghost btn--back" type="button" aria-label="Zurück">
                <span class="chevron" aria-hidden="true">‹</span><span class="btn__label">Zurück</span>
            </button>
            <h2>Sammlung</h2>
        </header>
        <p class="result-sub" id="collection-total"></p>
        <div class="history-list" id="chapter-list"></div>
    </section>

    <section class="screen screen--history" id="screen-chapter">
        <header class="bar">
            <button class="btn btn--ghost btn--back" type="button" aria-label="Zurück">
                <span class="chevron" aria-hidden="true">‹</span><span class="btn__label">Zurück</span>
            </button>
            <h2 id="chapter-title">Kapitel</h2>
        </header>
        <p class="result-sub" id="chapter-progress"></p>
        <div class="history-list" id="entry-list"></div>
    </section>
```

Der Sammlungsbildschirm führt mit der Handlung, nicht mit der Liste. Ergänze in
`#screen-collection` zwischen `#collection-total` und `#chapter-list`:

```html
        <p class="hint" id="collection-hint" role="status"></p>
        <!-- Die erste Handlung ist Weiterspielen, nicht Suchen. Ohne das
             kostet der nächste Eintrag drei Tipper und einen Blick durch zehn
             Kapitel. -->
        <button class="btn btn--primary btn--large btn--block" type="button" id="collection-continue" hidden></button>
```

Und in `#screen-chapter` hinter `#chapter-progress`:

```html
        <p class="hint" id="chapter-hint" role="status"></p>
```

Auf dem Startbildschirm **genau ein** Eintrag, in der Gruppe
`start-group-play` hinter `#daily-detail`:

```html
                    <button class="btn btn--on-dark btn--block" type="button" id="collection-button">Sammlung</button>
                    <p class="start-note" id="collection-detail" hidden></p>
```

> **Nicht mehr als das.** Die Gruppe „SPIELEN" trägt damit vier Knöpfe. Ein
> fünfter wäre der Anfang der Knopfwand, die heute erst beseitigt wurde.

- [ ] **Step 3b: Zieh die Ladeanzeige aus main.js heraus**

`setBusy` und `clearBusy` sind heute privat in `main.js`. Mit einem zweiten
Nutzer gehören sie in ein eigenes Modul — `client/js/ui/busy.js`:

```js
/**
 * Die Ladeanzeige, geteilt.
 *
 * Lag privat in main.js, solange nur das Erzeugen sie brauchte. Die Sammlung
 * erzeugt ebenfalls, und ein 5x5 schwer dauert gemessen 2,4 Sekunden - lange
 * genug, dass ein Tipp ohne Anzeige als nicht angekommen gelesen wird.
 */

import { el } from '../dom.js';

export function setBusy(text) {
    el('overlay-text').textContent = text;
    el('overlay').hidden = false;
}

export function clearBusy() {
    el('overlay').hidden = true;
}
```

Entferne die beiden Funktionen aus `main.js` und importiere sie stattdessen:

```js
import { clearBusy, setBusy } from './ui/busy.js';
```

- [ ] **Step 3c: Gib den Listenzeilen ihre Gestalt**

In `client/styles/screens.css`, bei den History-Regeln:

```css
/*
 * Eine Listenzeile ist ein Knopf, keine Karte.
 *
 * Als <button> erbt sie aus base.css, was auf iOS zaehlt: touch-action
 * manipulation (kein Doppeltipp-Zoom, keine 300-ms-Verzoegerung), kein grauer
 * Aufblitzer, kein Lange-druecken-Menue. Als <article> mit Klickhandler haette
 * sie nichts davon.
 */
.list-row {
    width: 100%;
    min-height: var(--tap-min);
    display: grid;
    gap: var(--space-1);
    text-align: left;
    border: var(--hairline) solid var(--line);
    border-radius: var(--radius);
    background: var(--surface);
    padding: var(--space-4) var(--space-5);
    transition: background var(--dur-fast) var(--ease-out);
}
/* Sichtbare Rueckmeldung: der Tipp arbeitet bis zu 2,4 Sekunden. */
.list-row:active { background: var(--surface-sunken); }
.list-row h3 { margin: 0; font-size: var(--text-body); }
.list-row.is-solved h3 { color: var(--muted); }

/* Ueberziehen bewegt die Liste, nicht die Seite darunter. */
#chapter-list, #entry-list { overscroll-behavior: contain; }
```

- [ ] **Step 4: Schreibe den Bildschirm**

`client/js/screens/collectionScreen.js`:

```js
/**
 * Die Sammlung: zehn Kapitel, je zwölf Einträge.
 *
 * Macht aus einem Generator, der unendlich viel kann, etwas Endliches - und
 * damit etwas, das man zu Ende bringen kann. Nichts ist gesperrt; der Pfeil
 * schlägt den nächsten offenen Eintrag vor, springen ist erlaubt.
 */

import { clear, el, make, setHint } from '../dom.js';
import { onEnter, showScreen } from '../router.js';
import { fetchBooklet } from '../api.js';
import { clearBusy, setBusy } from '../ui/busy.js';
import {
    chapterFor, chapters, newClueTypeAt, nextOpen, optionsFor, progressOf, totalProgress,
} from '../catalogue/catalogue.js';
import { cachedSolvedSeeds, loadSolvedSeeds } from '../catalogue/solvedSeeds.js';

let solved = new Set();
let openPlayFn = null;
let activePlayer = null;

/** Wurde für diesen Spieler schon einmal etwas geladen? */
let known = false;

/**
 * Die Zeile unter dem Knopf auf dem Startbildschirm.
 *
 * Bleibt verborgen, solange nichts bekannt ist. „0 von 120" wäre keine
 * Zurückhaltung, sondern eine Falschaussage: der Spieler hat vielleicht 40
 * gelöst, nur weiß dieses Gerät es noch nicht.
 */
export function renderCollectionNote(player) {
    const note = el('collection-detail');
    if (!player) { note.hidden = true; return; }
    solved = cachedSolvedSeeds(player.id);
    known = solved.size > 0;
    if (!known) { note.hidden = true; return; }
    const total = totalProgress(solved);
    note.hidden = false;
    note.textContent = `${total.solved} von ${total.total} gelöst`;
}

function chapterRow(chapter) {
    const part = progressOf(chapter, solved);
    const row = make('button', {
        className: 'list-row chapter-row',
        attrs: { type: 'button' },
    });
    row.append(make('h3', { text: chapter.title }));
    row.append(make('p', {
        className: 'history-card__score',
        text: `${part.solved}/${part.total}`,
    }));
    row.addEventListener('click', () => openChapter(chapter.themeId));
    return row;
}

function entryRow(chapter, entry, index) {
    const isSolved = solved.has(entry.seed);
    const row = make('button', {
        className: `list-row entry-row${isSolved ? ' is-solved' : ''}`,
        attrs: { type: 'button' },
    });
    row.append(make('h3', {
        text: `${entry.number}. ${entry.categoryCount}×${entry.valuesPerCategory} · ${entry.difficulty}`,
    }));
    const fresh = newClueTypeAt(chapter, index);
    if (fresh) row.append(make('p', { className: 'history-card__meta', text: `Neu: „${fresh}"` }));
    if (isSolved) row.append(make('p', { className: 'history-card__score', text: '✓ gelöst' }));
    row.addEventListener('click', () => play(chapter, entry));
    return row;
}

/** Welches Kapitel gerade offen ist, damit Zurück es neu zeichnen kann. */
let openThemeId = null;

/**
 * Erzeugt und öffnet einen Eintrag.
 *
 * Mit Overlay: 5×5 schwer braucht gemessen 2,4 Sekunden. Ohne Anzeige tippt
 * man und es geschieht scheinbar nichts - lange genug, um ein zweites Mal zu
 * tippen.
 */
async function play(chapter, entry) {
    setHint('chapter-hint', '');
    setBusy('Rätsel wird erzeugt …');
    try {
        const options = optionsFor(chapter, entry);
        const generated = await fetchBooklet(options);
        const puzzle = generated.booklet.puzzles[0];
        if (!puzzle) throw new Error('Dieses Rätsel konnte nicht erzeugt werden.');
        openPlayFn(puzzle, {
            mode: 'solo', player: activePlayer, options, puzzleIndex: 0,
        });
    } catch (error) {
        // Ein Eintrag, der nicht aufgeht, darf nicht das Kapitel blockieren.
        setHint('chapter-hint', error.message, true);
    } finally {
        clearBusy();
    }
}

function openChapter(themeId) {
    openThemeId = themeId;
    drawChapter();
    showScreen('screen-chapter');
}

function drawChapter() {
    const chapter = chapterFor(openThemeId);
    if (!chapter) return;
    el('chapter-title').textContent = chapter.title;
    const part = progressOf(chapter, solved);
    el('chapter-progress').textContent = `${part.solved} von ${part.total} gelöst`;
    const list = clear(el('entry-list'));
    chapter.entries.forEach((entry, index) => list.append(entryRow(chapter, entry, index)));
}

export async function openCollection(player) {
    activePlayer = player;
    showScreen('screen-collection');
    // Erst aus dem Zwischenspeicher, damit sofort etwas dasteht.
    solved = cachedSolvedSeeds(player.id);
    known = solved.size > 0;
    drawCollection();
    solved = await loadSolvedSeeds(player.id);
    known = true;
    drawCollection();
}

function drawCollection() {
    const total = totalProgress(solved);
    el('collection-total').textContent = known
        ? `${total.solved} von ${total.total} gelöst`
        : 'Wird geladen …';

    // Die erste Handlung ist Weiterspielen. Der Knopf nennt, wohin er führt.
    const chapter = chapters().find(candidate => nextOpen(candidate, solved));
    const button = el('collection-continue');
    if (chapter) {
        const entry = nextOpen(chapter, solved);
        button.hidden = false;
        button.textContent = `Weiter: ${chapter.title}, ${entry.number}`;
        button.onclick = () => play(chapter, entry);
    } else {
        button.hidden = false;
        button.textContent = 'Alles gelöst';
        button.disabled = true;
    }

    const list = clear(el('chapter-list'));
    for (const chapter of chapters()) list.append(chapterRow(chapter));
}

export function initCollection({ onOpenPlay }) {
    openPlayFn = onOpenPlay;

    /*
     * Beim Betreten neu zeichnen.
     *
     * Der Router poppt den Verlauf, also kommt man aus einem gelösten Rätsel
     * per Zurück genau hierher - und ohne das behauptete der Bildschirm, der
     * eben gelöste Eintrag sei offen.
     */
    onEnter(async id => {
        if (id !== 'screen-collection' && id !== 'screen-chapter') return;
        if (!activePlayer) return;
        solved = await loadSolvedSeeds(activePlayer.id);
        known = true;
        if (id === 'screen-collection') drawCollection(); else drawChapter();
    });
}
```

> **Achtung:** `setHint('chapter-hint', …)` braucht ein Element mit dieser
> Kennung. Ergänze in `#screen-chapter` hinter `#chapter-progress`:
> `<p class="hint" id="chapter-hint" role="status"></p>` — und ebenso
> `#collection-hint` in `#screen-collection`.

- [ ] **Step 5: Verdrahte es**

In `client/js/main.js`:

```js
import { initCollection, openCollection, renderCollectionNote } from './screens/collectionScreen.js';
```

In `wire()`:

```js
    initCollection({ onOpenPlay: openPlay });
    el('collection-button').addEventListener('click', () => {
        const player = getSelectedPlayer();
        if (player) openCollection(player);
    });
```

Und in `refreshStartScreen()`, am Ende:

```js
    renderCollectionNote(player);
```

- [ ] **Step 6: Nimm die Bildschirme in den Layout-Wächter**

Ergänze in `e2e/collection.spec.ts`:

```ts
import { findUndersizedControls, hasHorizontalScroll, PHONES } from './support/layoutGuard';

for (const phone of PHONES) {
  test(`${phone.name}: die Sammlung bleibt im Rahmen`, async ({ page }) => {
    await page.setViewportSize(phone.viewport);
    await ada(page, [1000001]);
    await page.goto('/');
    await page.locator('#collection-button').click();
    expect(await findUndersizedControls(page), 'Kapitelübersicht').toEqual([]);
    expect(await hasHorizontalScroll(page)).toBe(false);

    await page.locator('.chapter-row').first().click();
    expect(await findUndersizedControls(page), 'Kapitelliste').toEqual([]);
    expect(await hasHorizontalScroll(page)).toBe(false);
  });
}
```

- [ ] **Step 7: Lass alles laufen**

```bash
rtk npm test && rtk npx tsc --noEmit && rtk npx playwright test --reporter=line
```

- [ ] **Step 8: Prüfe den Ablauf selbst, nicht nur seine Teile**

Gehe diese Wege durch und halte fest, was dabei zu sehen ist:

1. Start → Sammlung → **Weiter**-Knopf. Erscheint das Overlay sofort? Landet man
   im richtigen Rätsel?
2. Rätsel lösen → Zurück. Steht der Haken? (Ohne den `onEnter`-Neuaufbau steht
   er nicht — das ist der Fehler, den dieser Schritt fangen soll.)
3. Kapitel → Eintrag → Zurück → anderer Eintrag. Bleibt die Scrollposition?
4. Randwisch-Geste statt Zurück-Knopf: dasselbe Ergebnis?
5. Startbildschirm: **vier** Knöpfe in SPIELEN, nicht fünf.

- [ ] **Step 9: Sieh es dir an**

Screenshots von Kapitelübersicht und Kapitelliste bei 390 × 844 **und** bei
320 × 568, hell und dunkel — und **sieh sie dir wirklich an**.

Der Wächter prüft Tapflächen und Überlauf; er sagt nichts darüber, ob zwölf
Zeilen mit demselben Titel lesbar sind. In diesem Projekt ist genau daran schon
zweimal etwas vorbeigelaufen: eine doppelte Überschrift und ein abgeschnittener
Titel bei 320 px, beide von Tests nicht zu sehen.

- [ ] **Step 10: Commit**

```bash
rtk git add -A && rtk git commit -m "feat(collection): ten chapters you can finish

The generator can make infinitely many puzzles, which is weaker than it sounds:
infinite means nothing is special and nothing is ever done. This turns it into
a booklet - 120 entries, each a chapter's rung on a climb from 3x4 leicht to
5x5 schwer.

Nothing is locked. The next open entry is suggested, not enforced; a collection
that pushes is the thing this app has spent its whole design avoiding.

Drawn twice on open: from the local cache first so something is there at once,
then from the server. Without a network the cached ticks stand and the entries
still play, because playing never needed a network."
```

---

### Task 6: Der Deploy-Prompt lernt den neuen Befehl

**Files:**
- Modify: `docs/superpowers/specs/2026-09-19-sammlung-design.md` (Hinweis auf den Befehl)

- [ ] **Step 1: Halte fest, was vor einem Deploy laufen muss**

Ergänze am Ende der Spec:

```markdown
## Vor jedem Deploy

Zusätzlich zu Tests und Typecheck:

    npm run catalogue:verify

Erwartet: `120 Einträge geprüft, 0 abweichend`. Schlägt es fehl, hat sich der
Generator geändert — dann wird der Katalog neu gebaut und die Änderung an den
Fingerprints bewusst abgenommen, weil sie bedeutet, dass Kapitel für alle
bisherigen Spieler andere Rätsel enthalten.
```

- [ ] **Step 2: Commit**

```bash
rtk git add docs/ && rtk git commit -m "docs: require the catalogue check before a deploy"
```

---

## Selbstprüfung des Plans

**Abdeckung der Spec.** Katalogeintrag und Kapitel-Rampe → Aufgabe 1. Bau statt
Würfeln, Fingerprint, Seed-Bereich → Aufgabe 1. Lesefunktionen, „Neu:"-Zeile,
kein Zwang → Aufgabe 2. Schnittstelle ohne Migration → Aufgabe 3. Offline und
Zwischenspeicher → Aufgaben 4 und 5. Bildschirme und Startbildschirm-Zeile →
Aufgabe 5. Unveränderlichkeits-Abzug und Generator-Bindung → Aufgabe 1.
Layout-Wächter → Aufgabe 5. Langsame Prüfung als eigener Befehl → Aufgaben 1
und 6. Keine Lücke.

**Platzhalter.** Keine. Jeder Code-Schritt trägt den einzusetzenden Text. Drei
Stellen sind ausdrücklich als „lies zuerst die vorhandene Datei" markiert, weil
dort ein bestehender Name übernommen werden muss statt erfunden: die
Kanonisierung in `canonicalPuzzle.ts`, der Einfüge-Helfer in
`sqlite-repositories.test.ts` und das Sammlungsfeld in `MemoryResults`.

**Namenskonsistenz.** `chapters`, `chapterFor`, `progressOf`, `totalProgress`,
`nextOpen`, `optionsFor`, `newClueTypeAt` werden in Aufgabe 2 definiert und in
Aufgabe 5 unter genau diesen Namen benutzt. `loadSolvedSeeds` und
`cachedSolvedSeeds` ebenso zwischen 4 und 5. `listSolvedSeeds(playerId,
minSeed)` ist in Aufgabe 3 auf allen drei Implementierungen gleich benannt.

**Bekannte Stolperstellen, im Text markiert.** Der Fingerprint muss dieselbe
Kanonisierung verwenden wie der Client, sonst ist er wertlos. Die
Kapitelliste zeigt zwölfmal denselben Titel — deshalb der ausdrückliche
Schritt, sie anzusehen statt nur zu messen.
